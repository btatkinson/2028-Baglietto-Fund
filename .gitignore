.env
__pycache__/
*.pyc
 
# captured snapshots + pasted payloads (data, not code)
data/
downloaded_files/

# minutes-model artifacts — all reproducible via warmup.py / train.py, never commit
mins_projection_model/data/
mins_projection_model/models/
mins_projection_model/catboost_info/
*.cbm
*.parquet

# generated report
out/report.html

# interactive-board cache (last pipeline's b365+proj+book; regenerated every run)
out/board_state.pkl