pandas>=2.0
requests>=2.28
python-dotenv>=1.0   # reads .env (your preferred pattern)
cryptography>=42.0   # RSA-PSS request signing for the Kalshi API (kalshi.py)
anthropic>=0.69      # Claude API for the LLM injury/news veto (news.py)
pyarrow>=12.0   # for parquet capture snapshots (optional; falls back to CSV)
flask>=3.0      # web UI (Railway deployment)
gunicorn>=21.0  # production server for the web UI
